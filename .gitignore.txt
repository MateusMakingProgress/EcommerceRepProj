# =======================================================================
# Visual Studio / Visual Studio Code / Rider (Geral)
# =======================================================================
.vs/
.vscode/
*.code-workspace
.DS_Store
Thumbs.db
.idea/
.idea_modules/
*.sln.iml
riderModule.iml
.rider/

# =======================================================================
# Build e Binários (Ajustado para os Microserviços no back-end)
# =======================================================================
# Ignora bin e obj dentro de qualquer pasta no back-end/src/
back-end/src/AuthMicroservice/**/bin/
back-end/src/CatalogService/**/bin/
back-end/src/AuthMicroservice/**/obj/
back-end/src/CatalogService/**/obj/
out/
*.exe
*.dll
*.so
*.dylib
*.lib
*.a

# =======================================================================
# Arquivos específicos de Usuário e Cache
# =======================================================================
back-end/src/**/*.csproj.user
*.rsuser
*.suo
*.user
*.userosscache
*.sln.docstates
*.userprefs

# =======================================================================
# Bancos de Dados Locais (SQLite dos Microserviços)
# =======================================================================
# Ignora os arquivos de banco de dados locais (.db, -shm, -wal) do Auth e Catalog
back-end/src/**/*.db
back-end/src/**/*.db-shm
back-end/src/**/*.db-wal

# =======================================================================
# Arquivos de Configuração e Ambiente (Segurança!)
# =======================================================================
.env
.env.local
.env.*.local

# Ignora os appsettings de Desenvolvimento/Locais de todos os microserviços
back-end/src/**/appsettings.Development.json
back-end/src/**/appsettings.Local.json

# Mantém o appsettings.json padrão (ele DEVE ir para o Git como base)
!back-end/src/**/appsettings.json

# =======================================================================
# Gerenciadores de Pacotes e Logs
# =======================================================================
*.nupkg
*.snupkg
.nuget/
packages/
.nuget/packages/
nuget.config
*.msbuild.log
*.log

# =======================================================================
# Ferramentas Adicionais (ReSharper, Coverage, Publish)
# =======================================================================
.resharper
.resharper.user
_ReSharper*/
*_ReSharper.sln
*.coverage
OpenCover/
TestResults/
*.trx
PublishProfiles/
publish/
PublishSettings/

# Entity Framework (Mantido caso precise ignorar diagramas)
*.edmx.diagram

# Uploads e arquivos temporários
uploads/
data/
*.tmp
*.temp
~$*